# Proceso — Crear schema

Drag/drop -> destinatario activo -> documento/página bajo puntero -> default schema -> ownerColor -> no-overlap -> state/snapshot -> render en página correcta. Obligatorio: schemaUid, type, documentId, pageNumber, geometry, ownerRecipientId, ownerColor.
