# 03 Schemas Y Plugin Registry

Schemas built-in y custom deben pasar por registry y contratos compartidos.

- `select` y `dropdown` resuelven al mismo contrato canónico.
- `radioGroup` y `checkboxGroup` exponen contratos de grupo con `groupId` y `optionId` estables.
