# Schema base

Todo schema debe conservar:

```txt
schemaUid
type
name/label
documentId
pageNumber
x/y/width/height
ownerRecipientId
ownerColor
required
readOnly
locked
hidden
__designer
```

## Identidad

`schemaUid` es la identidad técnica. `name`/`label` son visuales.
