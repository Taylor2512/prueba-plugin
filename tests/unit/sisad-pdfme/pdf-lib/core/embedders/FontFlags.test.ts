import { describe, it, expect } from 'vitest';
import * as moduleUnderTest from 'pdf-lib/core/embedders/FontFlags';

describe('sisad-pdfme/pdf-lib/core/embedders/FontFlags.ts', ()=>{
  it('imports without crashing', ()=>{
    expect(moduleUnderTest).toBeTruthy();
  });
});
