# Contrato documental

- `docs/**`: documentación pública.
- `ai/**`: operación IA.
- `reports/**`: evidencia extensa.
- `archive/**`: material historical o superseded.

Cada documento temporal declara `current`, `historical`, `superseded` o `external`.

Toda métrica incluye fecha o SHA. El estado actual no se mezcla con historia.
