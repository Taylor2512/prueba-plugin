status: PASS

# RTP-400 — PDF composition

- Append PDF implementado con orden explícito y ordenamiento configurable.
- Select-latest implementado.
- Manifest incluye artifactId, session, user/document, hash y pageRange.
- PDF tests: PASS — 2/2.
- Pendiente: canonical merge por schema delta y detección de colisión entre PDFs divergentes.
- Canonical delta merge añadido: deltas no colisionantes se combinan; writes divergentes emiten conflicto explícito.
- Canonical merge tests: PASS — 2/2.


> Estado declarado en reconciliación (RTP-545): **PASS** — append, select-latest y canonical delta merge implementados; el «Pendiente» queda resuelto en la línea siguiente del propio documento.
