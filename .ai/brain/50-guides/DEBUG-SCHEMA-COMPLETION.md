# Guide — debug schema completion

Para un schema incorrecto inspeccionar en orden:

1. manifest/capabilities;
2. access;
3. codec;
4. interaction state;
5. validation;
6. completion policy;
7. User projection.

No parchear `completed=true` en el renderer.
