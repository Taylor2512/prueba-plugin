# RightSidebar and inspector

Consume canonical selection, access, ownership and schema profile state. Keep one scroll owner per panel. Every interactive control needs a real read/write contract. Avoid duplicating labels, document/page metadata or action guards already owned by the canvas/command system.
