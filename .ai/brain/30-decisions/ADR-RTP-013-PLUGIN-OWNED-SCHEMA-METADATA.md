# ADR RTP-013 — Plugin-owned schema metadata

Decision: family, interaction kind, codec y completion metadata pertenecen al plugin/registry.

Runtime no mantiene sets manuales duplicados por schema type.
