---
id: TRC-015
campaign: TEMPLATE-RUNTIME-CONTRACT
status: READY
priority: P0
depends_on: ["TRC-014"]
autonomous: true
max_initial_source_files: 10
max_modified_product_files_per_slice: 4
max_search_rounds_before_hypothesis: 2
max_repair_rounds: 2
---

# TRC-015 — Complete SisadPdfmeGlobalConfig semantic namespaces

## Objective

Consolidate global config namespaces and remove ambiguous duplicated flags.

## Target namespaces

locale, ui, canvas, grid, snapping, guides, rulers, schemas, users, documents, collaboration, form, viewer, generator, data, signing, artifacts, accessibility, performance.

## Acceptance

- each capability has one authority namespace;
- duplicate readOnly flags across layers are replaced by explicit precedence contract;
- docs and validators align with effective runtime behavior.

## Evidence requirements

- config schema/type tests;
- compatibility map from legacy keys to canonical namespaces.

## Safety

Do not silently change behavior when consolidating keys.
