# Capability coverage matrix

Every capability must identify:
- definition;
- default;
- dependencies;
- surfaces;
- public controller/action;
- visibility;
- permission;
- runtime availability;
- dynamic update behavior;
- focal tests;
- integration tests.

Gate fails if a UI action bypasses capability resolution.
