---
id: TRC-016
campaign: TEMPLATE-RUNTIME-CONTRACT
status: PARTIAL
priority: P0
depends_on: ["TRC-015"]
autonomous: true
max_initial_source_files: 10
max_modified_product_files_per_slice: 4
max_search_rounds_before_hypothesis: 2
max_repair_rounds: 2
---

# TRC-016 — Explicit config precedence contract

## Objective

Make precedence deterministic and testable across safety constraints, surfaces, template settings and schema properties.

## Candidate precedence

hard safety constraint > surface override > instance configuration > template setting > schema property > default

## Acceptance

- precedence source-of-truth defined in one module;
- unit matrix covers Viewer/Form/Designer and capability-disabled cases;
- diagnostics include effective origin for denied actions when possible.

## Evidence requirements

- table-driven tests for precedence outcomes;
- at least one integration test per surface.

## Safety

No implicit precedence in scattered conditionals.

## Evidence executed — 2026-08-18 (PARTIAL)

- Existing config effect planning already computes deterministic change impact and no-op semantics from semantic hash/revision deltas.
- Focal tests:
	- `tests/unit/contracts/config/configEffectPlan.test.ts` PASS (6/6)
	- `tests/unit/contracts/config/configCompiler.test.ts` PASS (11/11)

### Why PARTIAL

- The explicit precedence ladder is now codified in `configPrecedence.ts`, including effective origin diagnostics.
- Cross-surface integration with Viewer/Form/Designer remains pending; this slice proves the authority and table-driven resolver only.

### Evidence executed — 2026-08-18 (slice 2)

- `tests/unit/contracts/config/configPrecedence.test.ts`: 3/3 PASS.
- `npm run typecheck` and focal ESLint: PASS.
