# Copy only reviewed keys into .codex/config.toml.
model_reasoning_effort = "low"
model_auto_compact_token_limit_scope = "body_after_prefix"

[features]
multi_agent = true
shell_snapshot = true

# Project memory remains in .ai/memory.
memories = false
