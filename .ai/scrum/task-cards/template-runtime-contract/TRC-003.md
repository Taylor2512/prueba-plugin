---
id: TRC-003
campaign: TEMPLATE-RUNTIME-CONTRACT
status: PARTIAL
priority: P0
depends_on: ["TRC-002"]
autonomous: true
max_initial_source_files: 12
max_modified_product_files_per_slice: 5
max_search_rounds_before_hypothesis: 2
max_repair_rounds: 2
---

# TRC-003 — Canonical portable Template JSON contract

## Objective

Define and validate a canonical JSON format with in-content schemaVersion and enough identity metadata for reliable reconstruction.

## Constraints

- schemaVersion must live inside JSON content;
- no filename-based versioning;
- no non-portable runtime objects in durable JSON;
- keep renderer projections derived from canonical portable shape.

## Acceptance

- canonical shape documented with required and optional fields;
- parser/validator path defined (parse -> identify -> version -> validate);
- compatibility policy for legacy shapes defined and testable;
- deterministic normalization rules for ordering documented.

## Required process

1. compare current template authority with target portable shape;
2. define migration boundaries and unsupported cases;
3. add validator tests for minimal/full/invalid cases;
4. map downstream dependencies (snapshot, import/export, schemas, assignments).

## Safety

Do not create parallel JSON format; evolve canonical authority in place.

## Evidence executed — 2026-08-18 (PARTIAL)

- `src/sisad-pdfme/shared/portableTemplate.ts` now enforces canonical payload envelope with:
	- `format: "sisad-pdfme-template"`
	- `schemaVersion: 1`
	- canonical sections `template/documents/users/schemas/assignments/settings`.
- Legacy shapes are migrated to canonical envelope via explicit migration path (`migrated: true`, outcome `MIGRATED`).
- Deterministic key ordering preserved for stable serialization.
- Focal tests PASS: `tests/unit/contracts/portableTemplate.test.ts`.

### Why PARTIAL

- Canonical field completeness for all schema families and full metadata contract still depends on TRC-004/TRC-005/TRC-022.
- Deep import/export UI workflow and package-level integration remain in TRC-028/TRC-030.

## Reconciliation appended — 2026-08-18

- TRC-004 residual field-matrix validation and TRC-005 registry serialization policy are now evidenced in their source cards and focal contract tests.
- The canonical parse → identify → migrate → validate → hydrate path remains the single authority; no second JSON format was introduced.
- Status remains `PARTIAL`: downstream import/export UI and package integration (TRC-028/TRC-030), plus full schema-family metadata (TRC-022), are still open acceptance dependencies.
