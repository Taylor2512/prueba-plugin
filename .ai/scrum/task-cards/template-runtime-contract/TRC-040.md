---
id: TRC-040
campaign: TEMPLATE-RUNTIME-CONTRACT
status: READY
priority: P1
depends_on: ["TRC-033","TRC-034"]
autonomous: true
max_initial_source_files: 8
max_modified_product_files_per_slice: 3
max_search_rounds_before_hypothesis: 2
max_repair_rounds: 2
---

# TRC-040 — Real shortcuts overlay E2E

## Objective

Close QH-015 gap with true overlay integration E2E, not fixture-shortened assertions.

## Required flow

click shortcuts trigger -> overlay visible -> search focus -> search lock term -> expected result -> internal scroll -> escape close -> focus return.

## Acceptance

- test runs against real mounted overlay path;
- fallback fixture path prohibited unless explicitly justified as auxiliary test.

## Safety

Do not reduce assertion scope to fit fixture limitations.
