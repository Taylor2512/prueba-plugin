---
id: TRC-017
campaign: TEMPLATE-RUNTIME-CONTRACT
status: PARTIAL
priority: P0
depends_on: ["TRC-003","TRC-012","TRC-004","TRC-008","TRC-014"]
autonomous: true
max_initial_source_files: 14
max_modified_product_files_per_slice: 5
max_search_rounds_before_hypothesis: 2
max_repair_rounds: 2
---

# TRC-017 — Aggregate Template JSON preflight validator

## Objective

Provide validateSisadPdfmeTemplate with structured issues for parse/version/entity/reference/portability failures.

## Issue shape

- code
- severity
- path
- entityId
- message
- hint?

## Minimum validations

JSON parse, schemaVersion, template identity, users, documents, schemas, assignments, registry type, aliases, duplicate IDs, orphan references, invalid pages/coordinates/options/signature/artifacts, unsupported config, non-serializable metadata.

## Acceptance

- validator returns deterministic structured issues;
- import pipeline consumes issues fail-closed;
- error codes documented and covered by contract tests.

## Safety

No silent repairs in validator path.

## Evidence executed — 2026-08-18 (PARTIAL)

- Added `validateSisadPdfmeTemplate` in `src/sisad-pdfme/shared/portableTemplate.ts` with structured issues:
	- `code`, `severity`, `path`, `entityId?`, `message`, `hint?`.
- Implemented deterministic validations for:
	- parse/object/json-safety
	- format/schemaVersion
	- required registry arrays
	- duplicate ids
	- orphan references (`documents`, `schemas`, `assignments` users/schemaUid).
- Tests extended in `tests/unit/contracts/portableTemplate.test.ts` for orphan-reference issue emission.

### Why PARTIAL

- Full matrix from card (geometry/options/signature/artifacts/alias-specific checks) is not yet complete.
- Harmonization with global workspace/template issue taxonomy remains pending.
