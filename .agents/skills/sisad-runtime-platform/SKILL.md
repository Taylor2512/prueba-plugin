---
name: sisad-runtime-platform
description: Hardening transversal del runtime SISAD-PDFME: Form, Viewer, PDF, Snapshot y lifecycle.
---

Reusar, no duplicar, skills existentes: `runtime-parity`, `sisad-form-viewer-parity`,
`sisad-schema-behavior-matrix`, `sisad-schema-family-refactor`, `sisad-public-api-compatibility`,
`sisad-react-performance`, `sisad-accessibility`, `quality-evidence`, `context-engineering`.

Workflow: characterize → authority → minimal change → focal gates → evidence → memory delta.
