# Ejemplos

## Scroll DetailView
`inspector-agent` + Claude + inspector context/rules/playbook.

## Toolbar cubre schema
`canvas-agent` y `interaction-agent` + Codex.

## AntD falla en Vitest
`test-infrastructure-agent` + Copilot.

## Docs inconsistentes
`docs-architecture-agent` + docs context/rules/playbook.
