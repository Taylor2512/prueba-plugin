import { describe, it, expect } from 'vitest';
import * as moduleUnderTest from 'pdf-lib/core/syntax/Whitespace';

describe('sisad-pdfme/pdf-lib/core/syntax/Whitespace.ts', ()=>{
  it('imports without crashing', ()=>{
    expect(moduleUnderTest).toBeTruthy();
  });
});
