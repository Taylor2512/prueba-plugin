# Schema Object Model

```txt
BaseSchema
→ Family schema
→ Plugin
→ Factory
→ Renderer
→ ValueAdapter
→ InspectorContract
→ Command handlers
```

Datos serializables. Comportamiento en plugins/adapters.
