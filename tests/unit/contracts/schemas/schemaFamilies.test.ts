import { describe, expect, it } from 'vitest';
import { INLINE_EDITABLE_TEXT_TYPES, resolveSchemaFamily, resolveSchemaSemanticFamily, resolveInspectorFamily, getSchemaTypeInspectorPreset } from '@sisad-pdfme/schemas/schemaFamilies';
describe('built-in schema registry matrix',()=>{
  it("text registry contract",()=>{expect(resolveSchemaFamily("text")).toBe("text");expect(resolveSchemaSemanticFamily("text")).toBe("text");expect(resolveInspectorFamily("text")).toBe("text-like");expect(getSchemaTypeInspectorPreset("text")).toBeDefined();});
  it("number registry contract",()=>{expect(resolveSchemaFamily("number")).toBe("text");expect(resolveSchemaSemanticFamily("number")).toBe("text");expect(resolveInspectorFamily("number")).toBe("text-like");expect(getSchemaTypeInspectorPreset("number")).toBeDefined();});
  it("multiVariableText registry contract",()=>{expect(resolveSchemaFamily("multiVariableText")).toBe("text");expect(resolveSchemaSemanticFamily("multiVariableText")).toBe("multiVariableText");expect(resolveInspectorFamily("multiVariableText")).toBe("text-like");expect(getSchemaTypeInspectorPreset("multiVariableText")).toBeDefined();});
  it("select registry contract",()=>{expect(resolveSchemaFamily("select")).toBe("text");expect(resolveSchemaSemanticFamily("select")).toBe("choice");expect(resolveInspectorFamily("select")).toBe("choice");expect(getSchemaTypeInspectorPreset("select")).toBeDefined();});
  it("dropdown registry contract",()=>{expect(resolveSchemaFamily("dropdown")).toBe("text");expect(resolveSchemaSemanticFamily("dropdown")).toBe("choice");expect(resolveInspectorFamily("dropdown")).toBe("choice");expect(getSchemaTypeInspectorPreset("dropdown")).toBeDefined();});
  it("date registry contract",()=>{expect(resolveSchemaFamily("date")).toBe("text");expect(resolveSchemaSemanticFamily("date")).toBe("dateTime");expect(resolveInspectorFamily("date")).toBe("text-like");expect(getSchemaTypeInspectorPreset("date")).toBeDefined();});
  it("time registry contract",()=>{expect(resolveSchemaFamily("time")).toBe("text");expect(resolveSchemaSemanticFamily("time")).toBe("dateTime");expect(resolveInspectorFamily("time")).toBe("text-like");expect(getSchemaTypeInspectorPreset("time")).toBeDefined();});
  it("dateTime registry contract",()=>{expect(resolveSchemaFamily("dateTime")).toBe("text");expect(resolveSchemaSemanticFamily("dateTime")).toBe("dateTime");expect(resolveInspectorFamily("dateTime")).toBe("text-like");expect(getSchemaTypeInspectorPreset("dateTime")).toBeDefined();});
  it("signature registry contract",()=>{expect(resolveSchemaFamily("signature")).toBe("text");expect(resolveSchemaSemanticFamily("signature")).toBe("signature");expect(resolveInspectorFamily("signature")).toBe("signature");expect(getSchemaTypeInspectorPreset("signature")).toBeDefined();});
  it("initials registry contract",()=>{expect(resolveSchemaFamily("initials")).toBe("text");expect(resolveSchemaSemanticFamily("initials")).toBe("signature");expect(resolveInspectorFamily("initials")).toBe("signature");expect(getSchemaTypeInspectorPreset("initials")).toBeDefined();});
  it("dateSigned registry contract",()=>{expect(resolveSchemaFamily("dateSigned")).toBe("text");expect(resolveSchemaSemanticFamily("dateSigned")).toBe("signature");expect(resolveInspectorFamily("dateSigned")).toBe("signature");expect(getSchemaTypeInspectorPreset("dateSigned")).toBeDefined();});
  it("fullName registry contract",()=>{expect(resolveSchemaFamily("fullName")).toBe("text");expect(resolveSchemaSemanticFamily("fullName")).toBe("text");expect(resolveInspectorFamily("fullName")).toBe("text-like");expect(getSchemaTypeInspectorPreset("fullName")).toBeDefined();});
  it("emailAddress registry contract",()=>{expect(resolveSchemaFamily("emailAddress")).toBe("text");expect(resolveSchemaSemanticFamily("emailAddress")).toBe("text");expect(resolveInspectorFamily("emailAddress")).toBe("text-like");expect(getSchemaTypeInspectorPreset("emailAddress")).toBeDefined();});
  it("company registry contract",()=>{expect(resolveSchemaFamily("company")).toBe("text");expect(resolveSchemaSemanticFamily("company")).toBe("text");expect(resolveInspectorFamily("company")).toBe("text-like");expect(getSchemaTypeInspectorPreset("company")).toBeDefined();});
  it("title registry contract",()=>{expect(resolveSchemaFamily("title")).toBe("text");expect(resolveSchemaSemanticFamily("title")).toBe("text");expect(resolveInspectorFamily("title")).toBe("text-like");expect(getSchemaTypeInspectorPreset("title")).toBeDefined();});
  it("attachment registry contract",()=>{expect(resolveSchemaFamily("attachment")).toBe("text");expect(resolveSchemaSemanticFamily("attachment")).toBe("action");expect(resolveInspectorFamily("attachment")).toBe("action");expect(getSchemaTypeInspectorPreset("attachment")).toBeDefined();});
  it("note registry contract",()=>{expect(resolveSchemaFamily("note")).toBe("text");expect(resolveSchemaSemanticFamily("note")).toBe("action");expect(resolveInspectorFamily("note")).toBe("action");expect(getSchemaTypeInspectorPreset("note")).toBeDefined();});
  it("approve registry contract",()=>{expect(resolveSchemaFamily("approve")).toBe("text");expect(resolveSchemaSemanticFamily("approve")).toBe("action");expect(resolveInspectorFamily("approve")).toBe("action");expect(getSchemaTypeInspectorPreset("approve")).toBeDefined();});
  it("decline registry contract",()=>{expect(resolveSchemaFamily("decline")).toBe("text");expect(resolveSchemaSemanticFamily("decline")).toBe("action");expect(resolveInspectorFamily("decline")).toBe("action");expect(getSchemaTypeInspectorPreset("decline")).toBeDefined();});
  it("checkbox registry contract",()=>{expect(resolveSchemaFamily("checkbox")).toBe("boolean");expect(resolveSchemaSemanticFamily("checkbox")).toBe("boolean");expect(resolveInspectorFamily("checkbox")).toBe("choice");expect(getSchemaTypeInspectorPreset("checkbox")).toBeDefined();});
  it("radioGroup registry contract",()=>{expect(resolveSchemaFamily("radioGroup")).toBe("boolean");expect(resolveSchemaSemanticFamily("radioGroup")).toBe("choice");expect(resolveInspectorFamily("radioGroup")).toBe("choice");expect(getSchemaTypeInspectorPreset("radioGroup")).toBeDefined();});
  it("checkboxGroup registry contract",()=>{expect(resolveSchemaFamily("checkboxGroup")).toBe("boolean");expect(resolveSchemaSemanticFamily("checkboxGroup")).toBe("choice");expect(resolveInspectorFamily("checkboxGroup")).toBe("choice");expect(getSchemaTypeInspectorPreset("checkboxGroup")).toBeDefined();});
  it("image registry contract",()=>{expect(resolveSchemaFamily("image")).toBe("mediaVisual");expect(resolveSchemaSemanticFamily("image")).toBe("media");expect(resolveInspectorFamily("image")).toBe("visual");expect(getSchemaTypeInspectorPreset("image")).toBeDefined();});
  it("svg registry contract",()=>{expect(resolveSchemaFamily("svg")).toBe("mediaVisual");expect(resolveSchemaSemanticFamily("svg")).toBe("media");expect(resolveInspectorFamily("svg")).toBe("visual");expect(getSchemaTypeInspectorPreset("svg")).toBeDefined();});
  it("table registry contract",()=>{expect(resolveSchemaFamily("table")).toBe("table");expect(resolveSchemaSemanticFamily("table")).toBe("table");expect(resolveInspectorFamily("table")).toBe("visual");expect(getSchemaTypeInspectorPreset("table")).toBeDefined();});
  it("line registry contract",()=>{expect(resolveSchemaFamily("line")).toBe("shapeBarcode");expect(resolveSchemaSemanticFamily("line")).toBe("shape");expect(resolveInspectorFamily("line")).toBe("visual");expect(getSchemaTypeInspectorPreset("line")).toBeDefined();});
  it("rectangle registry contract",()=>{expect(resolveSchemaFamily("rectangle")).toBe("shapeBarcode");expect(resolveSchemaSemanticFamily("rectangle")).toBe("shape");expect(resolveInspectorFamily("rectangle")).toBe("visual");expect(getSchemaTypeInspectorPreset("rectangle")).toBeDefined();});
  it("ellipse registry contract",()=>{expect(resolveSchemaFamily("ellipse")).toBe("shapeBarcode");expect(resolveSchemaSemanticFamily("ellipse")).toBe("shape");expect(resolveInspectorFamily("ellipse")).toBe("visual");expect(getSchemaTypeInspectorPreset("ellipse")).toBeDefined();});
  it("qrcode registry contract",()=>{expect(resolveSchemaFamily("qrcode")).toBe("shapeBarcode");expect(resolveSchemaSemanticFamily("qrcode")).toBe("barcode");expect(resolveInspectorFamily("qrcode")).toBe("visual");expect(getSchemaTypeInspectorPreset("qrcode")).toBeDefined();});
  it("japanpost registry contract",()=>{expect(resolveSchemaFamily("japanpost")).toBe("shapeBarcode");expect(resolveSchemaSemanticFamily("japanpost")).toBe("barcode");expect(resolveInspectorFamily("japanpost")).toBe("visual");expect(getSchemaTypeInspectorPreset("japanpost")).toBeDefined();});
  it("ean13 registry contract",()=>{expect(resolveSchemaFamily("ean13")).toBe("shapeBarcode");expect(resolveSchemaSemanticFamily("ean13")).toBe("barcode");expect(resolveInspectorFamily("ean13")).toBe("visual");expect(getSchemaTypeInspectorPreset("ean13")).toBeDefined();});
  it("ean8 registry contract",()=>{expect(resolveSchemaFamily("ean8")).toBe("shapeBarcode");expect(resolveSchemaSemanticFamily("ean8")).toBe("barcode");expect(resolveInspectorFamily("ean8")).toBe("visual");expect(getSchemaTypeInspectorPreset("ean8")).toBeDefined();});
  it("code39 registry contract",()=>{expect(resolveSchemaFamily("code39")).toBe("shapeBarcode");expect(resolveSchemaSemanticFamily("code39")).toBe("barcode");expect(resolveInspectorFamily("code39")).toBe("visual");expect(getSchemaTypeInspectorPreset("code39")).toBeDefined();});
  it("code128 registry contract",()=>{expect(resolveSchemaFamily("code128")).toBe("shapeBarcode");expect(resolveSchemaSemanticFamily("code128")).toBe("barcode");expect(resolveInspectorFamily("code128")).toBe("visual");expect(getSchemaTypeInspectorPreset("code128")).toBeDefined();});
  it("nw7 registry contract",()=>{expect(resolveSchemaFamily("nw7")).toBe("shapeBarcode");expect(resolveSchemaSemanticFamily("nw7")).toBe("barcode");expect(resolveInspectorFamily("nw7")).toBe("visual");expect(getSchemaTypeInspectorPreset("nw7")).toBeDefined();});
  it("itf14 registry contract",()=>{expect(resolveSchemaFamily("itf14")).toBe("shapeBarcode");expect(resolveSchemaSemanticFamily("itf14")).toBe("barcode");expect(resolveInspectorFamily("itf14")).toBe("visual");expect(getSchemaTypeInspectorPreset("itf14")).toBeDefined();});
  it("upca registry contract",()=>{expect(resolveSchemaFamily("upca")).toBe("shapeBarcode");expect(resolveSchemaSemanticFamily("upca")).toBe("barcode");expect(resolveInspectorFamily("upca")).toBe("visual");expect(getSchemaTypeInspectorPreset("upca")).toBeDefined();});
  it("upce registry contract",()=>{expect(resolveSchemaFamily("upce")).toBe("shapeBarcode");expect(resolveSchemaSemanticFamily("upce")).toBe("barcode");expect(resolveInspectorFamily("upce")).toBe("visual");expect(getSchemaTypeInspectorPreset("upce")).toBeDefined();});
  it("gs1datamatrix registry contract",()=>{expect(resolveSchemaFamily("gs1datamatrix")).toBe("shapeBarcode");expect(resolveSchemaSemanticFamily("gs1datamatrix")).toBe("barcode");expect(resolveInspectorFamily("gs1datamatrix")).toBe("visual");expect(getSchemaTypeInspectorPreset("gs1datamatrix")).toBeDefined();});
  it("pdf417 registry contract",()=>{expect(resolveSchemaFamily("pdf417")).toBe("shapeBarcode");expect(resolveSchemaSemanticFamily("pdf417")).toBe("barcode");expect(resolveInspectorFamily("pdf417")).toBe("visual");expect(getSchemaTypeInspectorPreset("pdf417")).toBeDefined();});
  it('unknown family falls back',()=>expect(resolveSchemaFamily('custom-unknown')).toBe('text'));
  it('text inline-editable',()=>expect(INLINE_EDITABLE_TEXT_TYPES.has('text')).toBe(true));
  it('select not inline-editable text',()=>expect(INLINE_EDITABLE_TEXT_TYPES.has('select')).toBe(false));
});
